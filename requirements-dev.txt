# Development / test-only dependencies. Not installed in the production image.
# Install with:  pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest==9.0.3
hypothesis==6.122.3
