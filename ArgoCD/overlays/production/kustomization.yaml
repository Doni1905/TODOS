apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: donee

resources:
  - ../../base

patches:
  - path: flask-deployment-patch.yaml
  - path: ingress-patch.yaml
  - path: mysql-statefulset-patch.yaml
