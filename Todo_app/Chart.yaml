apiVersion: v2
name: todo-app
description: A Helm chart for Flask Todo App with MySQL backend
type: application
# version is computed by CI from Conventional Commit messages (SemVer):
#   fix:/chore:/docs:/...  -> patch  (0.1.7 -> 0.1.8)
#   feat:                  -> minor  (0.1.7 -> 0.2.0)
#   feat!: / BREAKING CHANGE -> major (0.1.7 -> 1.0.0)
# The value below is only a placeholder; CI passes --version at package time
# and tags the commit vX.Y.Z. You do not need to bump this by hand.
version: 0.1.0
appVersion: "1.0.0"
keywords:
  - flask
  - todo
  - mysql
maintainers:
  - name: Doneeswaran
